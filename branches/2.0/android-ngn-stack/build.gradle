// 文件路径：`build.gradle`
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // 使用最新版本
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}